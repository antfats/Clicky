// import axios from "axios";

const BASEURL = "https://api.imgur.com/3/g/memes/viral/" + num;

    for (var i = 0; i < 10; i++) {
        function getRandomInt(max) {
            return Math.floor(Math.random() * Math.floor(max));
        }
        var num = getRandomInt(50)
        console.log(num)
        return num;
    }

